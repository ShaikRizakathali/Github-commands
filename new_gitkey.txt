ssh-keygen -t rsa -b 4060 -c "shaikhrizakathali@gmail.com"

eval "s(ssh-agent -s)

ssh-add ~/.ssh/id_rsa

cat~/.ssh/id_rsa.pub
    (then copy the content)

copy and paste the  ssh key on github/gitla   